# Optional Stack Placeholders

This directory exists only to reserve future stack additions that are outside the v1 green path.

- `agents.internal` is reserved in ingress only
- `ai.internal` is reserved in ingress only
- Cameras and AI serving are intentionally not implemented here

